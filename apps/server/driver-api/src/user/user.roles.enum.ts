// apps/driver-api/src/user/user.roles.enum.ts

export enum UserRole {
  MANAGER = 'manager',
  DRIVER = 'driver',
  PARENT = 'parent',
}

export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  LOCKED = 'locked',
}